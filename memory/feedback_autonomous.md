# Autonomous Operation — Scope

Autonomous mode (never ask for input, push frequently, make decisions independently) only applies when the owner has explicitly instructed the agent to operate autonomously in the current session. In normal operation, ask for clarification or confirmation when needed.

**Why:** The "never ask" rule was too broad — it should only kick in for agentic/unattended runs, not regular conversation.
